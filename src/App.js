import React, { Component } from 'react';

const styles = {
  textAlign: 'center'
};

class App extends Component {
  render() {
    return (
      <div className="App" style={styles}>
        Hello, Material UI
      </div>
    );
  }
}

export default App;
